export default function LearnQuestionLoading() {
  return (
    <main className="mx-auto max-w-2xl pb-16">
      <section className="glass-panel p-8 md:p-10">
        <p className="text-sm font-semibold uppercase tracking-[0.28em] text-[#84a8ff]">Preparing Questions</p>
        <h1 className="mt-4 text-3xl font-semibold tracking-tight text-white">Getting your next set ready.</h1>
        <p className="mt-4 text-sm leading-7 text-slate-300">
          This can take a few seconds on first load while we fetch and generate questions for your selected lane.
        </p>
      </section>
    </main>
  );
}
